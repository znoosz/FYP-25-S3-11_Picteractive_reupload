// Bridge entry so the Vite project can stay rooted in `client/`
// while the actual app source lives in the repo-level `src/`.
// This simply executes the real entry as a side effect.
import '../../src/main.jsx'

